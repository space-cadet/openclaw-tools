---
source_branch: main
source_commit: 530bd4d
---

# Session 2026-09-18 - Evening
*Created: 2026-09-18 21:53:08 IST*
*Last Updated: 2026-09-18 21:53:08 IST*

## Focus Task
T16: Token-Usage Parser — 9.x Version Awareness + SQLite Migration Fix
**Status**: ✅ COMPLETE

## Active Tasks
### T16: Token-Usage Parser — 9.x Version Awareness + SQLite Migration Fix
**Status**: ✅ COMPLETE
**Progress**:
1. ✅ Root-caused: nightly report returned 0 tokens after 9.x upgrade
2. ✅ Four bugs identified and fixed:
   - Stale version detection (Node v22 package.json instead of v24.21.0)
   - macOS SQLite URI mode failure on temp files
   - Timestamp Z-suffix mismatch (UTC vs IST string comparison)
   - No mtime pre-filter (scanned 5,600+ stale JSONL files)
3. ✅ Fixes applied: `_detect_openclaw_version()`, `detect_storage_backends()`, `_normalise_ts()`, plain `sqlite3.connect()`, mtime pre-filter
4. ✅ Verified: `--today` returns 855K input / $2.43, `--week` returns 26.3M / $35.82

### INFRA-11: Token-Usage Skill — 9.x SQLite Storage Migration Fix (workspace)
**Status**: ✅ COMPLETE
**Progress**:
1. ✅ Workspace task updated to COMPLETE
2. ✅ All acceptance criteria checked
3. ✅ Blocker resolved (was three separate bugs, not WAL/sidecar)

## Context and Working State
The token-usage skill's nightly report began returning 0 tokens after the OpenClaw 9.x upgrade. Root cause: the 9.x storage migration moved live session transcripts from loose `.jsonl` files into `openclaw-agent.sqlite` (`transcript_events.event_json`). The parser had no SQLite reader and no version detection to know it should read SQLite.

Key discovery: `exec` now refuses to open live OpenClaw state DBs directly. The sanctioned pattern is snapshot-to-`/tmp`. However, three additional bugs compounded:
1. Version detection read the old Node v22 `package.json` (2026.6.34) instead of the running Node v24.21.0 install (2026.9.4)
2. macOS `sqlite3.connect("file:...?mode=ro", uri=True)` fails on temp files
3. SQLite timestamps have `Z` suffix (UTC), `--since` is local IST — string comparison always False

## Critical Files
- `skills/token-usage/scripts/parse.py`: Parser with version detection, storage backend classifier, timestamp normaliser, macOS SQLite fix, mtime pre-filter
- `memory-bank/tasks/T16.md`: Full task documentation
- `~/.openclaw/agents/main/agent/openclaw-agent.sqlite`: Live transcript DB (protected state, snapshot-to-/tmp pattern)

## Session Notes
- mem-update protocol was NOT followed initially. Remediation applied:
  - Normalized openclaw-tools `tasks.md` to mb-core registry table format
  - Created this session file
  - Updated `edit_history.md` in both repos
  - Amended commit message to v6.12 format
- Both memory-banks updated: openclaw-tools (T16) and workspace (INFRA-11)
- Commit: `530bd4d` (to be amended with proper v6.12 message)

## mem-scan Remediation (23:45 IST)
- Deepak-approved: "fix all issues in the two mbs. Restore whatever is possible."
- Restored tasks/T1-T9.md from inline Task Details (details sections had preserved full checkbox records).
- Relabeled mislabeled T14 zstd section → T15; gave floating T13 bullets their proper heading.
- Regenerated edit_history.md from canonical chunks (was hand-edited — generated-view violation).
- Note: earlier "artifact rows" finding (Test/Tool Access) was a FALSE POSITIVE — those are content inside T7's K3 benchmark results table, not registry junk.
