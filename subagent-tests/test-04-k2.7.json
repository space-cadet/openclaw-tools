{
  "test_id": "test-04-k2.7",
  "test_name": "Parallel Subagent Spawn (K2.7)",
  "objective": "Check if multiple K2.7 subagents can run simultaneously",
  "spawn_config": {
    "task": "Calculate the square of this number: {{number}}",
    "model": "kimi/k2.7",
    "count": 3,
    "timeoutSeconds": 60
  },
  "expected": {
    "all_complete": true,
    "no_errors": true,
    "results_consistent": true
  },
  "failure_patterns": [
    "rate limit",
    "too many requests",
    "timeout",
    "401 Invalid Authentication"
  ],
  "detect": {
    "rate_limit": "Check for rate limit errors",
    "timeout": "Check if any subagent takes >60s",
    "auth_error": "Check for 401 errors in any subagent",
    "partial_failure": "Check if some subagents fail while others succeed"
  }
}
